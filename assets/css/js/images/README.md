# Visual Asset Requirements

For minimal loading speed and pure performance, graphical visual assets in the site hero sections are rendered natively using pure Inline SVG vector shapes within HTML5.

If adding external image assets in the future:
1. Place compressed PNG/WebP assets here.
2. Ensure images observe appropriate semantic `alt` tags within HTML.
